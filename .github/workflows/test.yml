name: test

on:
  push:
    branches:
      - main  # main ブランチへの push でトリガー

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.7", "3.8", "3.9", "3.10", "3.11"]

    steps:
      - uses: actions/checkout@v3  # リポジトリのコードをクローン

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}  # テストする Python のバージョンを指定

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip  # pip をアップグレード

      - name: Run the test
        run: |
          # nums1 ファイルの内容を標準入力として渡してテストを実行
          echo -e "100\n10" | python kadai1.py 
